name: CI – Lint, Build y Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-and-frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # --- BACKEND ---
      - name: Install backend dependencies
        working-directory: ./backend
        run: npm install || echo "instalación backend fallida (placeholder)"

      - name: Lint backend
        working-directory: ./backend
        run: npm run lint || echo "no hay lint todavía"

      - name: Test backend
        working-directory: ./backend
        run: npm test || echo "no hay tests todavía"

      # --- FRONTEND BUILD (placeholder) ---
      - name: Build frontend (placeholder)
        run: echo "Build frontend (placeholder)"




